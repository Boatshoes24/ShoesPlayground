local SPG, E, L, V, P, G = unpack(select(2, ...))
local tinsert = table.insert

SPG.profileStrings = {
    [1] = "Successfully applied profile(s) for |cffffff00%s|r",
    [2] = "|cffffff00%s|r profile does not exist. Aborting."
}